# Business LP screenshots

Set `NEXT_PUBLIC_BUSINESS_PREVIEW_MODE=screenshots` and place optimized WebP files here: `dashboard.webp`, `sponsored-meal.webp`, `seat-campaign.webp`, `x-share.webp`, and `analytics.webp`.

If an image cannot be loaded, the LP automatically uses its React mock preview.
