import Link from'next/link';import{getAdminLeads,getSalesFunnel}from '@/server/admin';const labels={NEW:'新規',CONTACTED:'連絡済み',QUALIFIED:'商談中',WON:'成約',LOST:'失注',ARCHIVED:'保管'} as const;export const metadata={title:'営業Lead管理'};export default async function Leads(){const[leads,funnel]=await Promise.all([getAdminLeads(),getSalesFunnel()]);const percent=(n:number)=>`${Math.round(n*100)}%`;return <section className="section"><h1>営業Lead</h1><div className="row wrap"><Link className="btn secondary small" href="/admin/growth">Growth Dashboardへ →</Link><Link className="btn secondary small" href="/admin/business">Business Dashboardへ →</Link></div><div className="analytics-grid"><div><strong>{funnel.counts.LP_VIEW??0}</strong><span>LP閲覧</span></div><div><strong>{funnel.leadTotal}</strong><span>問い合わせ</span></div><div><strong>{funnel.counts.SIGNUP_COMPLETED??0}</strong><span>登録</span></div><div><strong>{funnel.counts.CHECKOUT_STARTED??0}</strong><span>Checkout</span></div><div><strong>{funnel.counts.CHECKOUT_COMPLETED??0}</strong><span>決済完了</span></div></div><p className="muted">問い合わせ率 {percent(funnel.contactRate)} ・ 登録率 {percent(funnel.signupRate)} ・ Checkout到達率 {percent(funnel.checkoutRate)} ・ 決済完了率 {percent(funnel.paymentRate)} ・ Lead成約率 {percent(funnel.leadWinRate)}</p>{!leads.length?<div className="empty">まだ問い合わせはありません。</div>:<div className="comparison-scroll"><table><thead><tr><th>企業名</th><th>担当者</th><th>メール</th><th>エリア</th><th>目的</th><th>予算</th><th>流入元</th><th>状態</th><th>作成日時</th></tr></thead><tbody>{leads.map(l=><tr key={l.id}><td><Link className="text-link" href={`/admin/leads/${l.id}`}>{l.companyName}</Link></td><td>{l.contactName}</td><td>{l.email}</td><td>{l.area}</td><td>{l.purpose}</td><td>{l.monthlyBudgetRange??'—'}</td><td>{l.source}</td><td>{labels[l.status]}</td><td>{l.createdAt.toLocaleDateString('ja-JP')}</td></tr>)}</tbody></table></div>}</section>}
