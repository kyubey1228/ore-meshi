import{notFound}from'next/navigation';import{getAdminLead}from '@/server/admin';import{ActionForm}from '@/components/action-form';import{addLeadNote,convertLeadToBusiness,updateLeadStatus}from '@/server/actions/admin-leads';export default async function LeadDetail({params}:{params:Promise<{id:string}>}){const{id}=await params;const lead=await getAdminLead(id);if(!lead)notFound();return <section className="section narrow"><h1>{lead.companyName}</h1><div className="panel"><dl className="detail-list"><div><dt>担当者</dt><dd>{lead.contactName}</dd></div><div><dt>メール</dt><dd>{lead.email}</dd></div><div><dt>電話</dt><dd>{lead.phone??'—'}</dd></div><div><dt>Web</dt><dd>{lead.websiteUrl??'—'}</dd></div><div><dt>目的</dt><dd>{lead.purpose}</dd></div><div><dt>流入</dt><dd>{lead.source}</dd></div><div><dt>紹介コード</dt><dd>{lead.referralCode??'—'}</dd></div><div><dt>Partner</dt><dd>{lead.partnerCampaign?.title??'—'}</dd></div><div><dt>Business</dt><dd>{lead.businessAccount?.name??'未変換'}</dd></div></dl><p className="pre-wrap">{lead.message}</p></div><ActionForm label="状態を更新" action={form=>updateLeadStatus({id,status:form.get('status')})}><label>状態<select name="status" defaultValue={lead.status}>{['NEW','CONTACTED','QUALIFIED','WON','LOST','ARCHIVED'].map(s=><option key={s}>{s}</option>)}</select></label></ActionForm><ActionForm label="内部メモを追加" action={form=>addLeadNote({leadId:id,note:form.get('note')})}><label>内部メモ<textarea name="note" required maxLength={2000}/></label></ActionForm>{lead.notes.map(n=><div className="panel" key={n.id}><strong>{n.adminUser.displayName}</strong><small>{n.createdAt.toLocaleString('ja-JP')}</small><p>{n.note}</p></div>)}{!lead.businessAccountId&&<ActionForm label="Business下書きへ変換" confirm="BusinessAccountを作成しますか？" action={()=>convertLeadToBusiness(id)}/>}</section>}
