import { randomUUID } from 'crypto';
import { z } from 'zod';
import { NextResponse } from 'next/server';
import { prisma } from '@/lib/prisma';
const schema=z.object({eventType:z.enum(['LP_VIEW','BUSINESS_BANNER_IMPRESSION','BUSINESS_BANNER_CLICK','BUSINESS_LP_VIEW','SPONSORED_MEAL_SECTION_VIEW','SEAT_CAMPAIGN_SECTION_VIEW','PRICING_CTA_CLICK','CONTACT_CTA_CLICK','BUSINESS_SIGNUP_CTA_CLICK','PARTNER_CTA_CLICK','PRICING_VIEW','CONTACT_STARTED','SIGNUP_STARTED']),source:z.string().trim().max(100).optional(),medium:z.string().trim().max(100).optional(),campaign:z.string().trim().max(150).optional(),content:z.string().trim().max(150).optional(),referralCode:z.string().trim().max(50).optional()});
export async function POST(request:Request){try{const data=schema.parse(await request.json());const cookie=request.headers.get('cookie')??'';const existing=cookie.match(/(?:^|; )ore_business_marketing=([^;]+)/)?.[1];const sessionKey=existing?decodeURIComponent(existing):randomUUID();await prisma.businessMarketingEvent.create({data:{sessionKey,eventType:data.eventType,source:data.source,medium:data.medium,campaign:data.campaign,content:data.content}});const response=NextResponse.json({ok:true});response.cookies.set('ore_business_marketing',sessionKey,{httpOnly:true,sameSite:'lax',secure:process.env.NODE_ENV==='production',maxAge:60*60*24*90,path:'/'});if(data.referralCode)response.cookies.set('ore_business_ref',data.referralCode,{httpOnly:true,sameSite:'lax',secure:process.env.NODE_ENV==='production',maxAge:60*60*24*30,path:'/'});return response;}catch{return NextResponse.json({ok:false},{status:400});}}
