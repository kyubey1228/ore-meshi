import Link from 'next/link';
import { getMealList } from '@/lib/data';
import { paymentLabels } from '@/lib/format';
import { filterSchema } from '@/validators';
import { MealCard, Empty } from '@/components/meal-card';
export const metadata={title:'誰かの飯に乗っかる'};
export default async function Meals({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const raw=await searchParams;const parsed=filterSchema.safeParse(raw);const filters=parsed.success?parsed.data:{};const meals=await getMealList(filters);return <section className="section"><span className="eyebrow orange">FIND YOUR NEXT MEAL</span><h1>誰かの飯に乗っかる。</h1><p className="muted">今日の「うまい」を、一緒に。</p><form className="filter-bar"><label>いつ<input type="date" name="date" defaultValue={filters.date}/></label><label>どこ<input name="area" placeholder="例：新宿" maxLength={80} defaultValue={filters.area}/></label><label>予算の上限<select name="budget" defaultValue={filters.budget??''}><option value="">こだわらない</option><option value="1000">1,000円まで</option><option value="3000">3,000円まで</option><option value="5000">5,000円まで</option><option value="10000">10,000円まで</option></select></label><label>お会計<select name="paymentType" defaultValue={filters.paymentType}><option value="">こだわらない</option>{Object.entries(paymentLabels).map(([k,v])=><option key={k} value={k}>{v}</option>)}</select></label><button className="btn">飯を探す</button><Link className="text-link" href="/meals">リセット</Link></form>{!parsed.success&&<p role="alert" className="error">検索条件を確認してください。</p>}<p className="muted">{meals.length}件の飯 · 新しい募集から表示（最大100件）</p>{meals.length?<div className="meal-grid">{meals.map(meal=><MealCard key={meal.id} meal={meal}/>)}</div>:<Empty>今は誰も飯を募集してないみたい。</Empty>}</section>;}
