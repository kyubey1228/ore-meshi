import Link from 'next/link';
import { notFound } from 'next/navigation';
import { getMealById } from '@/lib/data';
import { currentUserId } from '@/server/auth';
import { candidateLabel, paymentLabels, yen, mealStatusLabels, requestStatusLabels, dateTimeLabel } from '@/lib/format';
import { UserAvatar } from '@/components/meal-card';
import { JoinForm, RequestDecision, CancelRequest, MealStatusControls } from '@/components/meal-controls';
export default async function MealDetail({params}:{params:Promise<{id:string}>}){const {id}=await params;const [meal,userId]=await Promise.all([getMealById(id),currentUserId()]);if(!meal)notFound();const host=meal.hostId===userId;const myRequest=meal.joinRequests.find(r=>r.userId===userId);return <section className="section narrow"><Link className="text-link" href="/meals">← 飯の一覧へ</Link><div className="panel detail"><span className="tag">{mealStatusLabels[meal.status]}</span><h1>{meal.title}</h1><Link className="person" href={`/users/${meal.host.id}`}><UserAvatar user={meal.host}/><span><strong>{meal.host.displayName}</strong><small>@{meal.host.twitterUsername}</small></span></Link><p className="pre-wrap">{meal.description}</p><dl className="detail-list"><div><dt>どこ</dt><dd>{meal.area}{meal.restaurant&&` / ${meal.restaurant}`}</dd></div><div><dt>いつ</dt><dd>{meal.candidates.map(c=><p key={c.id}>{candidateLabel(c)}</p>)}</dd></div><div><dt>いくら</dt><dd>{yen(meal.budgetMin)}〜{yen(meal.budgetMax)} / 人</dd></div><div><dt>お会計</dt><dd>{paymentLabels[meal.paymentType]}</dd></div><div><dt>何人</dt><dd>{meal._count.joinRequests+1} / {meal.maxParticipants}人（募集者含む）</dd></div>{meal.deadline&&<div><dt>締切</dt><dd>{dateTimeLabel(meal.deadline)}</dd></div>}{[{label:'ジャンル',value:meal.genre},{label:'お酒',value:meal.alcohol},{label:'たばこ',value:meal.smoking},{label:'年齢条件',value:meal.ageCondition}].filter(v=>v.value).map(v=><div key={v.label}><dt>{v.label}</dt><dd>{v.value}</dd></div>)}</dl>{meal.matches.map(m=><Link className="btn" key={m.id} href={`/matches/${m.id}`}>飯の予定を見る →</Link>)}</div>{host?<div className="panel"><h2>参加希望が届いてるよ</h2>{meal.joinRequests.length===0&&<p className="muted">まだ参加希望はありません。のんびり待とう。</p>}{meal.joinRequests.map(r=><article className="request" key={r.id}><Link className="person" href={`/users/${r.userId}`}><UserAvatar user={r.user}/><span>{r.user.displayName}<small>@{r.user.twitterUsername}</small></span></Link><p>{candidateLabel(r.candidate)}</p><p className="pre-wrap">{r.message}</p><p className="muted">{requestStatusLabels[r.status]}</p>{r.status==='PENDING'&&<RequestDecision id={r.id} mealId={id}/>}</article>)}{meal.status==='OPEN'&&meal.joinRequests.length===0&&<Link className="text-link" href={`/meals/${id}/edit`}>募集を編集する →</Link>}{meal.status!=='CANCELLED'&&<><hr/><MealStatusControls id={id}/><p className="muted">募集のキャンセルと、成立済みの飯の予定のキャンセルは別の操作です。</p></>}</div>:myRequest?<div className="panel"><h2>{requestStatusLabels[myRequest.status]}</h2><p>{candidateLabel(myRequest.candidate)}</p>{myRequest.status==='PENDING'&&<CancelRequest id={myRequest.id}/>}</div>:meal.status==='OPEN'?<div className="panel"><h2>この飯、一緒にどう？</h2>{userId?<JoinForm mealId={id} candidates={meal.candidates.map(c=>({id:c.id,label:candidateLabel(c)}))}/>:<Link className="btn" href="/login">Twitter/Xでログインして、この飯に行く</Link>}</div>:null}<div className="panel"><h2>募集している人</h2><p>{meal.host.bio||'気軽に一緒に飯いこう。'}</p><Link className="text-link" href={`/users/${meal.host.id}`}>プロフィールを見る →</Link></div></section>;}
