import { LoginButton } from '@/components/auth-buttons';
import { authConfigured } from '@/server/auth';
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;return <section className="narrow section center"><span className="empty-icon">🍚</span><h1 className="login-title">まずは、顔見知りになろう。</h1><p>Twitter/Xでログインして、飯の仲間を見つけよう。</p><LoginButton disabled={!authConfigured}/>{!authConfigured&&<p className="notice">ただいまログインの準備中です。公開中の募集はログインせずに見られます。</p>}{error&&<p role="alert" className="error">ログインできませんでした。もう一度お試しください。</p>}<p className="muted">許可なく投稿することはありません。<br/>Xの表示名・ユーザー名・プロフィール画像を利用します。</p></section>;}
