export const MIN_BUSINESS_SAMPLE_SIZE = 5;
export type OpportunityCell = { area: string; genre: string; demandIntents: number; activeMeals: number; matchedMeals: number; completedMeals: number; estimatedParticipants: number };
export function computeOpportunityScore(c: { demandIntents: number; activeMeals: number; completedMeals: number }) { const gap = Math.max(0, c.demandIntents - c.activeMeals); return c.demandIntents + gap * 2 + c.completedMeals; }
export function rankOpportunity(cells: OpportunityCell[], limit: number) { return cells.filter(c => c.demandIntents + c.activeMeals >= MIN_BUSINESS_SAMPLE_SIZE).map(c => ({ ...c, fillRate: c.activeMeals ? c.matchedMeals / c.activeMeals : 0, opportunityScore: computeOpportunityScore(c) })).sort((a, b) => b.opportunityScore - a.opportunityScore).slice(0, limit); }
export type SalesCopyInput = { area: string; genre: string; demandIntents: number; activeMeals: number; days: number };
export function generateSalesCopy(input: SalesCopyInput) { const gap=Math.max(0,input.demandIntents-input.activeMeals);const sufficient=input.demandIntents+input.activeMeals>=MIN_BUSINESS_SAMPLE_SIZE;const dataLine=sufficient?`直近${input.days}日間、${input.area}エリアでは${input.genre}カテゴリの食事需要が${input.demandIntents}件ありました(現在の募集は${input.activeMeals}件、不足${gap}件)。`:`${input.area}エリアで${input.genre}を含む食事相手を探しているユーザーがいます。`;return{contactForm:[`「俺は誰かと飯が食いたい！」と申します。${dataLine}`,'貴店を無料で掲載できます。まずは今日の空席だけ掲載して試すこともできます。','よろしければ詳細をお送りします。'].join('\n'),email:[`件名: ${input.area}エリアでの集客・空席対策のご案内`,'','はじめまして、「俺は誰かと飯が食いたい！」運営です。','',dataLine,'','無料で店舗ページを作成でき、有料機能は後から選べます。今日の空席だけ掲載して試すこともできます。','','ご興味があれば、下記より無料登録いただけます。','(登録URLはここに挿入)'].join('\n'),dm:[`【${input.area}】${dataLine}`,'無料で店舗ページを作成できます。今日の空席だけ掲載して試すこともできます。よければDMでご案内します。'].join('\n'),phoneScript:['お忙しいところ失礼します。「俺は誰かと飯が食いたい！」という飲食店向け集客サービスの者です。',dataLine,'無料で登録でき、今日の空席だけ掲載して試すこともできます。1分ほどお時間よろしいでしょうか？'].join('\n'),dataSufficient:sufficient}; }
